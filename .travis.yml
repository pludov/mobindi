language: node_js
node_js:
  - "iojs"
  - "8"
cache:
  directories:
    - "node_modules"
    - "ui/node_modules"
script: npm install && npm test && cd ui && npm install && CI=false npm run-script build